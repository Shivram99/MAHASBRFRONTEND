import { TransliterateDirective } from './transliterate.directive';

describe('TransliterateDirective', () => {
  it('should create an instance', () => {
    const directive = new TransliterateDirective();
    expect(directive).toBeTruthy();
  });
});
