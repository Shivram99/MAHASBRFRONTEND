import { AfterViewInit, Directive, ElementRef, HostListener, Input, OnInit, Optional, Self } from '@angular/core';

@Directive({
  selector: '[appTransliterate]',
  standalone: false
})
export class TransliterateDirective {
 
}