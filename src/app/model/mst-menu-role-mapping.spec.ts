import { MstMenuRoleMapping } from './mst-menu-role-mapping';

describe('MstMenuRoleMapping', () => {
  it('should create an instance', () => {
    expect(new MstMenuRoleMapping()).toBeTruthy();
  });
});
